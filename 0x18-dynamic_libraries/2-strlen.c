#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
* _strlen - length of string
* @s: the string
*
* Return: string len
*/

int _strlen(char *s)
{
	return (strlen(s));
}
