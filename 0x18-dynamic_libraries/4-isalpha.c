#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
* _isalpha - changes a string to an int
* @c: the string to be changed
*
* Return: the converted int
*/

int _isalpha(int c)
{
	return (isalpha(c));
}
