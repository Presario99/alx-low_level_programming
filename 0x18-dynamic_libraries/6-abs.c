#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
* _abs - absolute
* @n: the string to be changed
*
* Return: the converted int
*/

int _abs(int n)
{
	return (abs(n));
}
