#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
* _strcat - changes a string to an int
* @dest: destination
* @src: source
*
* Return: the converted int
*/
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
